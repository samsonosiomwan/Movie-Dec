import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
    text:{
        fontSize:20,
        fontWeight:"bold",
        textTransform:"capitalize",
        padding:"2%"
    },
    list:{
        marginTop:25,
    }
})